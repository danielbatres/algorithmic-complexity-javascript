// Big O calculation for time complexity

let bar = 'text' // O(1)

if () {} // O(1)

for () {} // O(n)

while() {} // O(n)

for() { for() {} } // O(n^2)

// Big O calculation for space complexity

let text = 'text' // O(1)

if () {} // O(1)

for() {} // O(1)

let result = [1, 2, ..., n] // O(n)

let dimensional = [ // O(n^2)
  [2, 4],
  [6, 8],
  [10, 12]
]

// Simplify the notation

// O(2n) ---> O(n)

// O(50) ---> O(1)

// O(n^2 + 50) ---> O(n^2)